// Create React App wants the frontend entry point to live at src/index.js,
// but since we also have a backend it made sense to put the "real" entry
// point inside the frontend folder. This just delegates down to that file.

import './frontend';
